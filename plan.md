# 内容分层、代码联动与阅读器体验整改计划

## Summary
- 保留测试文档、测试代码和测试数据，但生产环境完全隐藏，不删除仓库内容。
- 将仓库上级学习目录中文章涉及的代码纳入独立代码库，修复文章跳转后找不到代码的问题。
- 将检测、测验和习题从普通文章中拆出，通过文章内弹窗调用，不设置独立入口，也不保存答题记录。
- 修复 HTML 文件名和中文编码。
- 重做浅色 PDF 阅读器、统一密码的静态加密私密阅读器，并重点优化 TXT。
- 修复侧边栏频繁点击失效、页面切换后事件失效和遮罩层拦截点击的问题。

## Implementation Changes

### 1. 测试内容生产隐藏
- 增加统一内容角色：
  - `public`：正常公开；
  - `hidden`：保留源文件但不生成公开页面；
  - `test`：仅测试和本地开发使用；
  - `assessment`：由文章弹窗调用。
- 生产构建排除 `hidden` 和 `test` 内容，使其不进入：
  - 页面路由；
  - 文章列表和导航；
  - Pagefind 搜索；
  - RSS 和 sitemap；
  - 随机阅读；
  - 相关推荐。
- 保留测试源文件、fixtures、测试私密书籍和测试 HTML。
- 增加生成产物扫描，确保测试标题、测试 slug、测试路径和测试正文不会泄漏。

### 2. 文章—代码资源库
- 扫描 仓库上级学习目录，排除 `node_modules`、`dist`、缓存、自动生成文件和调试页面。
- 优先导入文章实际涉及的：
  - C/C++；
  - Python；
  - MATLAB/Simulink；
  - JavaScript/TypeScript；
  - FOC、电机控制、逆变器、仿真和 IMU 项目代码。
- 将代码复制到仓库独立目录，不依赖工作区外绝对路径。
- 为代码项目生成稳定 manifest，包含：
  - `codeId`；
  - 项目标题和说明；
  - 文件列表；
  - 编程语言；
  - 原始来源；
  - 文件哈希；
  - 可关联的行号范围。
- 文章 frontmatter 改为引用稳定 `codeId`、文件相对路径和行号范围。
- 增加代码库页面，提供：
  - 项目列表；
  - 文件树；
  - 语法高亮；
  - 行号定位；
  - 复制代码；
  - 原始文件下载；
  - 与文章的反向关联。
- 文章和代码双向联动：
  - 文章滚动到代码说明段落时自动打开对应文件并高亮；
  - 代码面板定位时反向标记对应文章段落；
  - 文件不存在、行号失效时显示明确错误；
  - 桌面端使用双栏；
  - 移动端使用“文章/代码”切换抽屉。

### 3. 检测、测验、习题弹窗化
- 不再把检测、测验、习题生成普通文章。
- 独立存放在 `content/assessments/`，通过稳定 `assessmentId` 被文章引用。
- 文章内显示“开始检测”“章节练习”“查看例题”等按钮。
- 点击后在当前页面打开模态弹窗，不跳转、不改变 URL。
- 弹窗支持：
  - 单选；
  - 多选；
  - 判断；
  - 填空；
  - 代码阅读题；
  - 公式题；
  - 答案和解析。
- 不记录：
  - 完成状态；
  - 总分；
  - 错题；
  - 最近答题时间；
  - 历史答题记录。
- 只保留当前弹窗会话中的临时状态：
  - 用户主动提交后显示即时正误；
  - 显示正确答案和解析；
  - 支持上一题、下一题和重新作答；
  - 关闭或刷新后清空。
- 支持直接查看解析，不强制答题。
- 桌面端使用居中弹窗，移动端使用近全屏底部弹层。
- assessment 内容不进入文章搜索、文章列表、导航、RSS、sitemap 和随机阅读。
- 构建阶段校验文章引用的 `assessmentId` 和题目结构，避免失效按钮和空弹窗。

### 4. 侧边栏点击失效修复
- 同时检查以下侧边栏体系：
  - 知识树侧栏；
  - 文章目录侧栏；
  - 导航菜单侧栏；
  - 移动端抽屉和遮罩层。
- 根因整改：
  - 将事件绑定从易被 Swup 替换的局部 DOM，迁移到稳定外壳或使用事件委托；
  - 每个 controller 使用唯一生命周期和 `AbortController`；
  - 页面切换、返回和重复进入时先销毁旧监听器、观察器、定时器和旧 DOM 引用；
  - 禁止重复注册 click、keydown、pointerdown 和 Swup hook；
  - 关闭抽屉时同步清除 `aria-expanded`、`aria-hidden`、body 滚动锁和遮罩状态；
  - 修复残留遮罩、透明层、`pointer-events` 和 z-index 导致的点击拦截；
  - 导航点击在页面过渡开始前关闭菜单，但不移除目标链接的默认行为；
  - 页面切换失败或取消时恢复侧栏可交互状态；
  - 防止快速连续点击造成“假关闭”“旧页面监听器抢事件”或按钮永久 disabled。
- 交互细节：
  - 点击侧栏外部关闭；
  - `Escape` 关闭；
  - 点击当前项目不会被错误禁用；
  - 触摸设备使用 pointer 事件，避免 click 延迟和重复触发；
  - 键盘 Tab 焦点限制在打开的抽屉内，关闭后返回触发按钮；
  - 页面加载、Swup 切换和浏览器后退后均可正常点击。
- 增加事件生命周期诊断属性，测试环境可以检测监听器和控制器是否重复累积。

### 5. HTML 文件名、编码和导入
- 所有 HTML 源文件统一以 UTF-8 读取、规范化和输出。
- URL 使用稳定 ASCII slug，页面标题使用可读中文。
- 优先从 `<title>`、主标题和父目录恢复显示名称；无法恢复时使用人工可读名称。
- 从 仓库上级学习目录 精选 3–5 个无需后端即可运行的技术页面，优先：
  - IMU 仪表盘；
  - FOC/电机控制生成文档；
  - 控制算法和仿真页面。
- 复制必要 CSS、JS、图片和字体，并重写相对资源路径。
- 排除依赖目录、缓存、调试页和依赖服务器的页面。
- 提供沙箱预览、原始页面、来源说明和资源加载失败提示。

### 6. PDF 阅读器
- 修复 PDF 文件 URL、PDF.js module、worker、字体和 CMap 的生产路径。
- 加载前验证文件存在、响应类型正确，并处理中文文件名和 URL 编码。
- 默认使用浅色纸张效果：
  - PDF 页面白底；
  - 外围浅灰或暖灰；
  - 不再使用固定黑色/深灰阅读背景；
  - 深色仅作为外围可选主题。
- 默认连续纵向阅读，同时支持单页视图。
- 工具栏支持：
  - 上一页/下一页；
  - 页码输入跳转；
  - 当前页/总页数；
  - 适合宽度；
  - 适合页面；
  - 固定缩放；
  - 单页/连续视图；
  - 全屏；
  - 原始文件下载。
- 支持方向键、PageUp、PageDown、Home、End。
- 记忆当前页、缩放和视图模式。
- 使用可取消渲染任务和可见页优先渲染，避免快速跳页错乱和大文件卡顿。
- 区分文件不存在、路径错误、文件损坏和 PDF.js 初始化失败，并提供可操作提示。

### 7. 私密阅读器与静态云端加密
- 私密密码固定使用 `123`，不依赖环境变量。
- 加密阶段在本地脚本完成，浏览器端只使用用户输入的密码解密。
- 仓库和 Pages 产物只保存：
  - manifest；
  - 加密 TXT/EPUB 分段；
  - 加密图片；
  - 加密元数据。
- 每本书使用独立随机 `bookSalt`，正文、图片和分段使用独立 IV。
- 继续使用 AES-GCM + PBKDF2-SHA256 v2 格式。
- 前端一次输入密码后自动完成入口、书架、元数据和正文解锁。
- 密码不写入 URL、HTML、localStorage 或 sessionStorage；只保留当前标签页内存状态。
- 错误密码立即清理派生 key 和缓存，并允许重试。
- TXT 优先优化：
  - UTF-8、UTF-8 BOM、GBK、GB18030 自动识别；
  - 保留换行、缩进、空行和段落；
  - 分段解密；
  - 合理正文宽度和段间距；
  - 可调字号、行高、字体、边距和主题；
  - 章节导航、前后章节、进度和滚动位置恢复；
  - 移动端无横向滚动。
- EPUB 支持常见 XHTML 图片、相对路径、URL 编码路径和常见 MIME；图片失败只显示占位提示，不让整章白屏。
- 无原始文件的旧书不强行删除或覆盖，先生成迁移报告。

### 8. 其他体验整改
- 将本次涉及的 Astro、TypeScript、Markdown、JSON 和 HTML 文件统一规范为 UTF-8。
- 修复乱码按钮、状态文本、无障碍标签和错误信息。
- 清理空壳按钮；无法实现的功能隐藏或显示明确状态。
- 统一所有阅读器的加载、空内容、错误、重试和返回体验。
- 检查移动端工具栏、侧栏、弹窗、全屏、触摸滚动和键盘焦点。
- 控制新增静态资源体积，避免 GitHub Pages 再次因大产物部署超时。

## Test Plan

### 内容和安全
- 测试内容不出现在生产路由、搜索、RSS、sitemap、导航和随机阅读。
- 仓库和生产产物中不存在私密 TXT/EPUB 明文正文和明文图片。
- 使用密码 `123` 可以解锁统一迁移后的 TXT、EPUB、目录、正文和图片。

### 代码联动
- 每个 `codeId`、文件和行号范围都能解析。
- 文章滚动能高亮代码，代码定位能反向标记文章。
- 生产构建不依赖仓库外部学习目录的绝对路径。
- 复制、下载、文件切换、移动端抽屉均可用。

### 测验弹窗
- 文章按钮打开正确题目。
- 作答、即时反馈、解析、上一题、下一题和关闭均可用。
- 关闭或刷新后不保留得分、错题、完成状态和答题时间。
- 题目不会生成独立文章或独立入口。

### 侧边栏
- 连续快速点击、重复打开/关闭、点击遮罩、按 Escape、键盘 Tab 和浏览器后退均正常。
- Swup 连续切换至少 10 次后，侧栏仍可点击。
- 验证没有重复监听器、残留遮罩、body 滚动锁或永久 disabled 状态。
- 桌面端和移动端分别执行真实浏览器测试。

### PDF、HTML、私密阅读
- 真实 PDF 验证加载、首屏、连续/单页视图、跳页、缩放、全屏、键盘和状态恢复。
- HTML 验证中文标题、ASCII slug、CSS/JS/图片资源、沙箱和原始页面。
- TXT 验证多种编码、长文本、章节、换行、分页、进度和移动端布局。
- EPUB 验证常见图片、图片失败降级、章节切换和错误密码重试。

### 回归与上线
- 运行类型检查、单元测试、生产 E2E、安全扫描、路由完整性检查和完整构建。
- 生产预览中实际操作首页、侧栏、代码联动、测验弹窗、HTML、PDF、TXT 和 EPUB。
- 推送后验证 GitHub Pages 构建成功、首页和关键功能路由返回 `200`。

## Assumptions
- 私密密码固定为 `123`，这是便利优先方案；静态客户端解密无法防御知道密码或具备开发者工具能力的用户。
- 密文版本提交到仓库，明文源文件只用于本地加密和构建，不进入公开产物。
- 测验只作为文章内弹窗，不设置独立页面或导航入口。
- 测验状态只存在当前弹窗会话中，关闭或刷新后全部清除。
- 代码只导入与文章有关或技术价值明确的部分，不复制整个学习目录。
- PDF 默认浅色连续阅读，单页和深色外围主题为可选模式。
