---
interface Props {
	class: string;
	pagefindBody?: boolean;
}
const { class: className, pagefindBody = true } = Astro.props;
---
<div data-pagefind-body={pagefindBody ? true : undefined} class={`prose dark:prose-invert prose-base max-w-none! custom-md ${className}`}>
    <slot/>
</div>
