---
date: 2026-06-06
section: 电机控制
chapter: hardware
chapterTitle: 硬件与驱动
chapterOrder: 20
category: 硬件与驱动
source: motor
visibility: public
title: HW-01 电机本体：电驱算法的物理基础
tags:
  - motor-control
status: learning
summary: "**副标题：从磁路到绕组，理解PMSM的物理本质**"
navGroup: 基础与硬件
navGroupOrder: 20
---

# HW-01 电机本体：电驱算法的物理基础

**副标题：从磁路到绕组，理解PMSM的物理本质**

---

## 1.  核心摘要  

**一句话讲清楚**：电机本体是电驱系统的"心脏"，理解其物理特性是开发高质量控制算法的前提——电感决定电流响应速度，磁链决定转矩常数，电阻决定铜损，极对数决定电角速度与机械角速度的关系。

**认知挂钩**：很多人以为电机就是"通电就转"，**这是致命误区！** 实际上，电机是一个**复杂的机电能量转换系统**：电能→磁场→机械能。不理解磁路、绕组、反电动势等物理概念，就无法理解FOC算法中的Park变换、Id/Iq解耦、弱磁控制等核心概念。

**与算法的关联**：
-  **算法关联**：电感(Ld, Lq) → 决定电流环PI参数设计 → `Kp = L × ωc`，电感不准则PI参数整定失败
-  **算法关联**：磁链(ψf) → 决定转矩常数 → `Kt = 1.5 × P × ψf`，磁链不准则转矩输出偏差大
-  **算法关联**：极对数(P) → 决定Park变换角度 → `θe = P × θm`，极对数错则Park变换完全错误
- 电阻(Rs) → 决定铜损 → 影响效率计算
- 齿槽转矩 → 决定转矩脉动 → 影响控制精度

---

## 2.  问题引入  

### 工程师的真实困惑

**场景1：FOC控制参数整定困难**

> 工程师A:"我按照教科书上的公式计算了电流环PI参数,但电机还是震荡..."
>
> **问题现象:**
> - 电流环震荡,无法稳定
> - 调小Kp,响应变慢
> - 调大Kp,震荡加剧
> - 低速时转矩脉动大

**场景2：弱磁控制失效**

> 工程师B:"我想让电机超速运行,但一进入弱磁区就报错..."
>
> **问题现象:**
> - 高速时电流失控
> - Id电流无法正常调节
> - 电机发热严重

**场景3：转矩常数不准**

> 工程师C:"我测量的转矩常数和电机参数表不一致..."
>
> **问题现象:**
> - 实际输出转矩与计算值偏差大
> - 不同工作点转矩常数变化
> - 效率计算不准确

### 核心问题

这些问题的根本原因是什么？

**答案**：不理解电机本体的物理特性！

- 电流环震荡 → 不理解电感参数(Ld, Lq)对电流响应的影响
- 弱磁失效 → 不理解d轴电感和磁链的关系
- 转矩不准 → 不理解磁阻转矩和转矩常数

### 学习目标

读完本模块，你将能够：

 **理解PMSM的物理结构** - 定子、转子、绕组、永磁体
 **掌握关键参数** - 电感(Ld, Lq)、磁链(ψf)、电阻(Rs)、极对数(P)
 **理解数学模型** - dq坐标系下的电压方程和转矩方程
 **建立软硬件关联** - 理解电机参数如何影响控制算法
 **解决实际问题** - 诊断和解决FOC控制中的常见问题

---

## 3.  直观理解  

### 类比1：电机就像"旋转的磁铁"

**生活场景**：想象你有两块磁铁，一块固定(定子)，一块可以旋转(转子)。

```plain
定子磁铁(N极) ────→ 吸引 ────→ 转子磁铁(S极)
                              ↓
                           旋转对齐
```

**关键理解**：
- 定子产生旋转磁场(通过三相绕组)
- 转子跟随旋转磁场(永磁体)
- 控制定子磁场方向 → 控制转子位置

| 特性 | 直流电机 | PMSM |
|------|---------|------|
| 换向方式 | 机械换向器 | 电子换向(FOC) |
| 磁场来源 | 电枢绕组 | 永磁体 |
| 控制复杂度 | 简单 | 复杂(需要Park变换) |
| 效率 | 较低 | 高 |

### 类比2：电感就像"惯性"

**生活场景**：想象你在推一个重物，刚开始很难推动，但一旦动起来，很难停下来。

> 推力(电压) ────→ 重物(电感) ────→ 速度(电流)
>   │                │                │
>   └──── 惯性 ──────┘
>
> **数学关系:**
> $V = L \times di/dt$    $F = m \times dv/dt$ (牛顿第二定律)
>
> $V$(电压)  $F$(力)
> $L$(电感)  $m$(质量)
> $i$(电流)  $v$(速度)

**关键理解**：电感阻碍电流变化，就像惯性阻碍速度变化。电感越大，电流变化越慢，电流环带宽受电感限制。

### 类比3：极对数就像"齿轮比"

**生活场景**：想象自行车的齿轮，前齿轮转一圈，后齿轮转多圈。

> 机械旋转 ────→ 极对数P ────> 电角度
>   │                            │
>   1圈                         P圈
>
> **示例(4极电机P=2):**
> 机械角度: $0° → 180°$
> 电角度:   $0° → 360°$
> 关系: $\theta_e = P \times \theta_m = 2 \times 180° = 360°$

**关键理解**：极对数 = 电角度/机械角度的比值，Park变换需要电角度。

### 关键概念速查

**电感(Ld, Lq)**：
- Ld: d轴电感，影响Id电流响应
- Lq: q轴电感，影响Iq电流响应
- 表贴式PMSM: Ld ≈ Lq
- 内置式PMSM: Lq > Ld(磁阻转矩)

**磁链(ψf)**：永磁体产生的磁通量，决定反电动势和转矩常数，单位韦伯(Wb)

**极对数(P)**：转子磁极对数(N+S为一对)，决定电角度与机械角度关系，θe = P × θm

**电阻(Rs)**：定子绕组电阻，决定铜损，影响效率

---

## 4.  技术原理  

### 4.1 PMSM结构原理

#### 4.1.1 基本结构

```plain
PMSM（永磁同步电机）结构：

                    定子（Stator）
         ┌─────────────────────────────────┐
         │         ┌───────────┐           │
         │    A    │           │    A'     │  A相绕组
         │   ┌──┐  │    转子    │  ┌──┐     │
         │   │  │  │  (Rotor)  │  │  │     │
         │   └──┘  │   ┌───┐   │  └──┘     │
         │    B    │   │ N │   │    B'     │  B相绕组
         │   ┌──┐  │   │   │   │  ┌──┐     │
         │   │  │  │   │ S │   │  │  │     │
         │   └──┘  │   └───┘   │  └──┘     │
         │    C    │           │    C'     │  C相绕组
         └─────────────────────────────────┘

关键部件：
  1. 定子（Stator）：硅钢片叠压，嵌入三相绕组
  2. 转子（Rotor）：永磁体产生磁场
  3. 绕组（Winding）：三相绕组（A、B、C）
  4. 永磁体（PM）：钕铁硼或铁氧体
```

#### 4.1.2 定子绕组

```plain
三相绕组空间分布（2极电机）：

                0° (A相轴)
                  │
         240°    │    120°
          (B相)  │   (C相)
            \    │    /
             \   │   /
              \  │  /
               \ │ /
                ─┼─
                 │
               转子

电角度关系：
  A相：θe = 0°
  B相：θe = 120°
  C相：θe = 240°

星形连接（Y接）——大多数PMSM采用：
  相电压 = 线电压/√3，相电流 = 线电流
  优点：中性点可引出，无环流，绝缘要求较低
```

#### 4.1.3 转子结构

> 类型1：表贴式（SPM）
>   特点：Ld ≈ Lq，适用于高速应用，但离心力大
>
> 类型2：内置式（IPM）
>   特点：Ld < Lq，可利用磁阻转矩，结构强度高
>
> 类型3：埋入式
>   特点：Ld << Lq，磁阻转矩大，适合弱磁控制

### 4.2 电机参数详解

#### 4.2.1 电感参数

> **电感的定义：**
> $L = N \times \Phi / I$
>
> **电感对电流的影响：**
> $V = L \times dI/dt$
>
> 电流变化率 = 电压 / 电感
>
> **示例：**
> $L = 1\text{mH}, V = 100\text{V}$
> $dI/dt = 100,000 \text{ A/s} = 100 \text{ A/ms}$
> 要使电流增加10A，需要时间：$t = 0.1\text{ms} = 100\mu\text{s}$

**Ld和Lq的区别**：

> d轴（直轴）：与转子磁场方向一致
> q轴（交轴）：与转子磁场方向垂直
>
> **磁路分析：**
> d轴方向：永磁体磁阻大（永磁体磁导率≈空气）
> q轴方向：铁芯磁阻小（铁芯磁导率高）
> 因此：$L_d < L_q$
>
> SPM电机：$L_d \approx L_q$（磁路对称）
> IPM电机：$L_d < L_q$（通常 $L_q = 1.5 \sim 3 \times L_d$）

**电感对控制的影响**：

┌──────────────────────────────────────────────────────────────┐
│  参数          │ SPM (Ld=Lq)      │ IPM (Ld<Lq)              │
│  ─────────────┼─────────────────┼──────────────────────────│
│  电流响应      │ Id, Iq相同       │ Id响应快(Ld小),Iq慢(Lq大)│
│  磁阻转矩      │ 无               │ 有(与(Lq-Ld)×Id×Iq成正比)│
│  弱磁能力      │ 差               │ 好(利用Ld×Id产生弱磁)    │
│  电流环设计    │ 单一带宽         │ Id, Iq可能需要不同带宽   │
└──────────────────────────────────────────────────────────────┘

**电感测量方法**：

> 方法1：静态测量（锁定转子）
>   1. 将转子锁定在d轴方向
>   2. 施加高频交流电压，测量电流响应
>   3. 计算：$L_d = V / (2\pi f \times I)$
>   4. 旋转90°电角度，重复测量得到$L_q$
>
> 方法2：高频注入法
>   注入高频电压信号$V_{d\_hf} = V_h \times \sin(\omega_h \times t)$
>   测量电流响应：
>     $L_d = V_h / (\omega_h \times I_{d\_hf\_amplitude})$
>     $L_q = V_h / (\omega_h \times I_{q\_hf\_amplitude})$
>
> 方法3：从电机参数表获取
>   小功率伺服电机：$L_d, L_q = 0.1 ~ 10 \text{ mH}$
>   中功率电机：$L_d, L_q = 1 ~ 50 \text{ mH}$
>   大功率电机：$L_d, L_q = 10 ~ 100 \text{ mH}$

#### 4.2.2 磁链参数

> 磁链的定义：
>   $\psi = N \times \Phi$

永磁体磁链（$\psi_f$）：
  决定反电动势大小、转矩常数、弱磁电流需求

磁链与反电动势的关系：
  $e_a = \psi_f \times \omega_e \times \sin(\theta_e)$

  反电动势常数：$K_e = \psi_f \times P$

  手推计算示例：
    额定转速 $n = 3000 \text{ rpm}$, 额定反电动势 $E = 200\text{V}$, 极对数 $P = 4$
    $\omega_m = 2\pi \times 3000 / 60 = 314.16 \text{ rad/s}$
    $K_e = E / \omega_m = 0.637 \text{ V·s/rad}$
    $\psi_f = K_e / P = 0.159 \text{ Wb} = 159 \text{ mWb}$

磁链与转矩的关系：
  SPM电机：$T_e = 1.5 \times P \times \psi_f \times I_q$
  IPM电机：$T_e = 1.5 \times P \times [\psi_f \times I_q + (L_d - L_q) \times I_d \times I_q]$
                                       ↑永磁转矩    ↑磁阻转矩

  转矩常数：
    SPM：$K_t = 1.5 \times P \times \psi_f$
    IPM：$K_t = 1.5 \times P \times [\psi_f + (L_d - L_q) \times I_d]$

  $K_t$与$K_e$的关系：
    三相PMSM（FOC控制）：$K_t = 1.5 \times K_e$（三相合力矩与单相反电动势的关系）
    直流有刷电机：$K_t = K_e$（单位制数值相等）
    注：PMSM的$K_t=(3/2)\cdot P\cdot\psi_f$，$K_{e\_phase\_peak}=P\cdot\psi_f$，故$K_t=1.5K_e$

  手推计算示例：
    $P = 4, \psi_f = 0.15 \text{ Wb}, I_q = 10\text{A}$
    $T_e = 1.5 \times 4 \times 0.15 \times 10 = 9 \text{ N·m}$
    $K_t = 1.5 \times 4 \times 0.15 = 0.9 \text{ N·m/A}$

#### 4.2.3 电阻参数

> 电阻的物理意义：
>   $R_s = \rho \times l / A$
>
>   对电机的影响：
>   1. 铜损：$P_{cu} = 3 \times I^2 \times R_s$
>   2. 电压降：$V = I \times R_s$
>   3. 效率：$\eta = P_{out} / (P_{out} + P_{cu} + P_{fe} + ...)$
>
>   电阻随温度变化：
>     $R_s(T) = R_s(20°C) \times [1 + \alpha \times (T - 20)]$
>     $\alpha$ = 铜的温度系数 $\approx 0.00393$ /°C
>
>     示例：$R_s(20°C) = 1\Omega, T = 100°C$
>     $R_s(100°C) = 1 \times [1 + 0.00393 \times 80] = 1.315\Omega$
>     温升80°C，电阻增加31.5%！
>
>   典型值：
>     小功率电机：$R_s = 0.1 ~ 10 \text{ \Omega}$
>     中功率电机：$R_s = 0.01 ~ 1 \text{ \Omega}$
>     大功率电机：$R_s = 0.001 ~ 0.1 \text{ \Omega}$

#### 4.2.4 极对数

> 极对数的定义：$P = \text{极数} / 2$
>
> 电角度与机械角度：$\theta_e = P \times \theta_m$
> 电角速度与机械角速度：$\omega_e = P \times \omega_m$
>
> 同步转速：$n_s = 60 \times f / P$
>
> 极对数选择原则：
>   • 高速应用：少极数（2极、4极）
>   • 低速大转矩：多极数（8极、12极、16极）
>   • 齿槽转矩考虑：槽数与极数配合

### 4.3 电机数学模型

#### 4.3.1 三相静止坐标系（abc）

> 三相电压方程：
>   $u_a = R_s \times i_a + d\psi_a/dt$
>   $u_b = R_s \times i_b + d\psi_b/dt$
>   $u_c = R_s \times i_c + d\psi_c/dt$
>
> 问题：方程复杂，有互感项，电感随位置变化，三相耦合难以独立控制
> 解决方案：坐标变换

#### 4.3.2 两相静止坐标系（αβ）——Clarke变换

> Clarke变换（abc → αβ）：
>   $i_\alpha = i_a$
>   $i_\beta = (i_a + 2 \times i_b) / \sqrt{3}$
>
> 反变换（αβ → abc）：
>   $i_a = i_\alpha$
>   $i_b = -i_\alpha/2 + \sqrt{3}/2 \times i_\beta$
>   $i_c = -i_\alpha/2 - \sqrt{3}/2 \times i_\beta$
>
> 优点：减少变量数量（3→2），但电感仍随位置变化

#### 4.3.3 两相旋转坐标系（dq）——Park变换

> Park变换（αβ → dq）：
>   $i_d =  i_\alpha \times \cos(\theta_e) + i_\beta \times \sin(\theta_e)$
>   $i_q = -i_\alpha \times \sin(\theta_e) + i_\beta \times \cos(\theta_e)$
>
> 反变换（dq → αβ）：
>   $i_\alpha = i_d \times \cos(\theta_e) - i_q \times \sin(\theta_e)$
>   $i_\beta = i_d \times \sin(\theta_e) + i_q \times \cos(\theta_e)$
>
> dq坐标系下的电压方程：
>   $u_d = R_s \times i_d + L_d \times d(i_d)/dt - \omega_e \times L_q \times i_q$
>   $u_q = R_s \times i_q + L_q \times d(i_q)/dt + \omega_e \times (L_d \times i_d + \psi_f)$
>
> 关键洞察：
>   1. d轴与转子磁场方向一致
>   2. q轴与转子磁场方向垂直
>   3. id控制磁场（弱磁/增磁）
>   4. iq控制转矩
>
> dq坐标系下的转矩方程：
>   $T_e = 1.5 \times P \times [\psi_f \times i_q + (L_d - L_q) \times i_d \times i_q]$
>
> 优点：电感为常数（不随位置变化），id和iq解耦，便于控制设计

### 4.4 电机参数汇总

```plain
┌──────────────────────────────────────────────────────────────────┐
│  参数          │ 符号  │ 单位    │ 典型值         │ 测量方法     │
│  ─────────────┼──────┼────────┼───────────────┼──────────────│
│  定子电阻      │ Rs   │ Ω      │ 0.01 ~ 10     │ 直流测量     │
│  d轴电感       │ Ld   │ mH     │ 0.1 ~ 50      │ 锁定转子/注入│
│  q轴电感       │ Lq   │ mH     │ 0.1 ~ 100     │ 锁定转子/注入│
│  永磁体磁链    │ ψf   │ Wb     │ 0.01 ~ 1      │ 空载反电动势 │
│  极对数        │ P    │ -      │ 1 ~ 20        │ 计数磁极     │
│  转动惯量      │ J    │ kg·m²  │ 0.001 ~ 10    │ 自由停机法   │
│  摩擦系数      │ B    │ N·m·s  │ 0.001 ~ 1     │ 空载测试     │
└──────────────────────────────────────────────────────────────────┘
```

### 4.5 电机特性曲线

#### 4.5.1 转矩-转速特性

```plain
PMSM转矩-转速曲线：

转矩 T
  │ 恒转矩区          弱磁区
  │←─────────────→│←──────────────────→│
  │  ─────────────┐                    │
  │               │＼                   │
  │               │  ＼                 │
  │               │    ＼               │
  └───────────────┼──────────────┼──────→ 转速 n
               基速 nb        最高转速 nmax

恒转矩区：Id = 0(SPM)或最优Id(IPM)，Iq = 额定电流
弱磁区：Id < 0(负电流去磁)，转矩随转速下降，功率近似恒定

基速计算：nb = (60 / 2π) · Vdc / (√3 · Ke)  [rpm]
注：若Ke单位为V/(rad/s)，需乘60/(2π)≈9.549转换为rpm。若Ke单位为V/rpm，公式简化为nb=Vdc/(√3·Ke)
```

#### 4.5.2 效率曲线

> 损耗构成：$P_{loss} = P_{cu} + P_{fe} + P_{mech} + P_{stray}$
> 效率：$\eta = P_{out} / (P_{out} + P_{loss})$
>
> 典型效率：
>   小功率电机：75% ~ 85%
>   中功率电机：85% ~ 92%
>   大功率电机：92% ~ 96%

#### 4.5.3 电流轨迹（Id-Iq平面）

> 工作区域：
>   1. 恒转矩区：电流极限圆内
>   2. 弱磁区：电压极限圆内
>   3. 最大转矩/电流比轨迹（MTPA）
>
> MTPA轨迹（IPM电机）：
>   $I_d = (\psi_f - \sqrt{\psi_f^2 + 4 \times (L_q-L_d)^2 \times I_q^2}) / (2 \times (L_q-L_d))$
>
> 弱磁轨迹：
>   $I_d = -(\psi_f/L_d) + \sqrt{(u_{max}/\omega_e)^2 - (L_q \times I_q)^2} / L_d$

---

## 5.  交叉视角  

> 电机参数不是孤立的硬件指标，它们直接决定了控制算法的行为。本章节揭示硬件参数与算法之间的深层关联。

### 5.1 电感参数(Ld, Lq) → 电流环PI参数设计

**硬件特性**：
> 电感决定电流变化速度：
>   $di/dt = V / L$
>   $L$大 → 电流变化慢 → 带宽低
>   $L$小 → 电流变化快 → 带宽高

** 算法关联**：电感直接决定电流环PI参数
> 电流环PI参数计算：
>   $K_p = L \times \omega_c$ ($\omega_c$ = 电流环带宽)
>   $K_i = R \times \omega_c$
>
>   示例：
>     $L_d = 1\text{mH}, L_q = 1.5\text{mH}, R = 0.1\Omega$
>     $\omega_c = 1000 \text{ rad/s}$ (约160Hz)
>     $K_{p\_d} = 1\text{mH} \times 1000 = 1$
>     $K_{p\_q} = 1.5\text{mH} \times 1000 = 1.5$
>     $K_i = 0.1 \times 1000 = 100$

**参数整定指南**：

> 现象：电流环震荡
>
> **诊断：**
> 1. 检查电感参数是否准确
> 2. 检查PI参数是否过大
>
> **解决：**
> - 降低Kp(电感估计偏大)
> - 增加滤波器
> - 降低电流环带宽

### 5.2 磁链参数(ψf) → 转矩常数与Id=0控制

**硬件特性**：
> 转矩常数：$K_t = 1.5 \times P \times \psi_f$
> 反电动势常数：$K_e = P \times \psi_f$
> 关系：$K_t = K_e$（单位统一后）

** 算法关联**：磁链决定转矩常数，直接影响Id=0控制策略
> Id=0控制策略：
>   $T_e = 1.5 \times P \times \psi_f \times I_q$
>
>   如果$\psi_f$不准：
>     - 转矩输出偏差
>     - 效率计算错误
>     - 弱磁控制失效
>
> 软件应对：
>   1. 离线辨识$\psi_f$
>   2. 在线自适应
>   3. 查表补偿

**参数辨识方法**：
```c
// 方法1:空载反电动势法
float psi_f = V_phase / (P * omega_e);

// 方法2:堵转测试法
float psi_f = T_measured / (1.5 * P * Iq);
```

### 5.3 极对数(P) → Park变换角度计算

**硬件特性**：
> 电角度计算：$\theta_e = P \times \theta_m$
> 示例：4极电机(P=2)，机械角度180° → 电角度360°

** 算法关联**：极对数决定Park变换中的电角度
```c
void Park_Transform(float alpha, float beta, float theta_e, float *d, float *q)
{
    float sin_theta = sinf(theta_e);
    float cos_theta = cosf(theta_e);

    *d =  alpha * cos_theta + beta * sin_theta;
    *q = -alpha * sin_theta + beta * cos_theta;
}

// 如果极对数错误：
//   - Park变换角度错误
//   - Id/Iq解耦失败
//   - 电流环震荡
```

**常见错误**：

> **错误1:极对数配置错误**
>
> - 现象:电机抖动,无法正常运转
> - 解决:检查电机铭牌,确认极对数
>
> **错误2:角度单位混淆**
>
> - 现象:Park变换结果错误
> - 解决:统一使用弧度制

### 5.4 电阻(Rs) → 效率计算与温度补偿

> 铜损计算：$P_{cu} = 3 \times I^2 \times R_s$
> 示例：$I = 10\text{A}, R_s = 0.1\Omega \rightarrow P_{cu} = 30\text{W}$
>
> 如果$R_s$不准：
>   - 效率计算错误
>   - 温升估计错误
>   - 电流环Ki参数偏差

### 5.5 磁阻转矩 → MTPA控制策略

> IPM电机：$L_q > L_d$
>   $T_e = 1.5 \times P \times [\psi_f \times I_q + (L_d - L_q) \times I_d \times I_q]$
>                        ↑永磁转矩         ↑磁阻转矩
>
> MTPA控制(最大转矩电流比)：
>   利用磁阻转矩，提高效率
>   $I_{d\_ref} = f(I_{q\_ref}, L_d, L_q, \psi_f)$
>
> 控制策略选择：
>   SPM($L_d \approx L_q$)：Id=0
>   IPM($L_q > L_d$)：MTPA、弱磁控制

### 5.6 参数测量方法

**离线测量**：
```c
float Rs = V_dc / I_dc;                              // 电阻：直流法
float Ld = V_d_high_freq / (2*pi*f * I_d_high_freq); // 电感：高频注入法
float psi_f = V_phase / (P * omega_e);               // 磁链：空载反电动势法
```

**在线辨识**：
```c
// 递推最小二乘法(RLS)实时更新电机参数
void RLS_Update(float Vd, float Vq, float Id, float Iq, float omega_e)
{
    // 状态方程：V = R*I + L*di/dt + omega_e*psi
    // 递推更新参数估计
}
```

---

## 6.  工程案例  

### 案例1：电流环震荡——电感参数不准

**项目背景**：

> 应用:电动自行车
> 电机:表贴式PMSM,额定功率500W
> 控制器:STM32G4,FOC控制
> 问题:电流环震荡,无法稳定

**诊断过程**：

> 步骤1:示波器观察电流波形 → 震荡频率约200Hz
> 步骤2:检查电流环带宽 → 设计160Hz,接近震荡频率
> 步骤3:检查电感参数 → 数据表L=0.5mH,实测L=0.8mH(偏差60%!)
> 步骤4:重新计算PI参数 → 原参数基于错误电感值

**根本原因**：电感参数不准！Kp计算偏小 → 带宽不足 → 相位裕度小 → 震荡

**解决方案**：

> **方案A:修正电感参数,重新计算PI**
>
> - 新Kp = 0.8mH × 1000 = 0.8 (原0.5)
> - 结果:震荡消除,响应良好 
>
> **方案B:降低电流环带宽**
>
> - ωc = 800 rad/s (原1000)
> - 结果:震荡消除,但响应变慢 

**经验总结**：
1. 电机参数必须实测，不能只看数据表
2. 电感测量方法：高频注入法(在线)、LCR电桥(离线)
3. 电流环带宽应小于电频率的1/10
4. 相位裕度应>45°

---

### 案例2：弱磁控制失效——d轴电感参数不准

**项目背景**：

> 应用:电动汽车
> 电机:内置式PMSM,额定功率30kW
> 控制器:TI C2000,FOC+弱磁控制
> 问题:高速时电流失控,报过流故障

**诊断过程**：

> 步骤1:分析弱磁控制策略 → 采用前馈弱磁控制
> 步骤2:检查Id电流轨迹 → Id未按预期负值增加
> 步骤3:检查电压极限圆 → 电压已达极限,但Id未调整
> 步骤4:检查d轴电感参数 → 数据表Ld=1mH,实测Ld=1.5mH

**根本原因**：d轴电感参数不准！弱磁电流计算错误：

> $I_{d\_weak} = -\psi_f / L_d$
>
> - 计算值(基于1mH): $I_d = -100\text{A}$
> - 实际需要(基于1.5mH): $I_d = -67\text{A}$
> - 结果:弱磁电流过大 → 饱和 → 失控

**解决方案**：

> - 方案A:修正电感参数 → 弱磁控制正常 
> - 方案B:采用自适应弱磁控制(在线调整Id,基于电压反馈) → 鲁棒性更好 

**经验总结**：
1. 弱磁控制对电感参数敏感
2. 内置式PMSM必须准确测量Ld, Lq
3. 推荐自适应弱磁控制策略
4. 电压极限圆和电流极限圆必须正确绘制

---

### 案例3：转矩常数不准——定义混淆

**项目背景**：

> 应用:工业机器人关节
> 电机:表贴式PMSM,额定转矩10Nm
> 控制器:STM32H7,FOC控制
> 问题:实际输出转矩与指令偏差大(约16%)

**诊断过程**：

> 步骤1:数据表Kt = 1.0 Nm/A,计算Te = 1.0 × 5 = 5Nm
> 步骤2:实测Te = 4.2Nm
> 步骤3:反推Kt_actual = 4.2 / 5 = 0.84 Nm/A
> 步骤4:发现数据表Kt与FOC公式Kt = 1.5 × P × ψf不一致!

**根本原因**：转矩常数定义混淆！

> - 数据表$K_t = 1.0 \text{ Nm/A}$ (基于峰值相电流)
> - FOC中$K_t = 1.5 \times P \times \psi_f$ (基于$I_q$电流)
> - 转换：$K_{t\_FOC} = K_{t\_datasheet} / \sqrt{3/2} = 1.0 / 1.225 = 0.816 \text{ Nm/A}$
> - 与实测0.84接近！

**解决方案**：修正Kt_FOC = 0.84 Nm/A，转矩精度提升到±2% 

**经验总结**：
1. 注意转矩常数的定义(峰值电流 vs RMS电流)
2. FOC中转矩公式：Te = 1.5 × P × ψf × Iq
3. 推荐实测转矩常数
4. 不同工作点可能有偏差(饱和效应)

---

### 案例4：极对数配置错误

**项目背景**：

> 应用:无人机电机
> 电机:外转子PMSM,极数未知
> 控制器:STM32G4,FOC控制
> 问题:电机抖动,无法正常运转

**诊断过程**：

> 步骤1:检查编码器 → 正常
> 步骤2:检查Park变换角度 → 角度变化异常
> 步骤3:检查极对数配置 → 配置P=4(8极电机)
> 步骤4:手动旋转一圈,电角度变化720° → P=2(4极电机)!

**根本原因**：极对数配置错误！Park变换角度错误 → Id/Iq解耦失败 → 电流环震荡

**解决方案**：修正P=2，电机运行正常 

**经验总结**：
1. 极对数必须准确配置
2. 测量方法：手动旋转一圈，观察电角度变化
3. 注意：极数 = 2P(N+S)，极对数 = P

---

### 案例5：电机选型流程

> 步骤1：确定负载需求（转矩、转速、惯量、工作制）
> 步骤2：计算功率需求 $P = T \times \omega / \eta$
> 步骤3：选择电机类型
> - 高精度伺服 → PMSM
> - 低成本驱动 → BLDC或感应电机
> - 高速应用 → SPM
> - 大转矩应用 → IPM
> 步骤4：确定额定参数（功率、转矩、转速、峰值转矩）
> 步骤5：校验惯量匹配 $J_{motor} / J_{load} = 1 ~ 5$
> 步骤6：校验热容量 $I_{eq} = \sqrt{\sum(I_i^2 \times t_i) / T_{cycle}} \leq I_n$

---

## 7.  实践练习  

### 练习1：计算题——电感参数与PI参数

> 已知PMSM参数：
> - $L_d = 1\text{mH}, L_q = 1.5\text{mH}, R_s = 0.1\Omega$, 额定电流 = 10A
>
> **要求：**
> 1. 计算电流环PI参数(带宽$\omega_c = 1000 \text{ rad/s}$)
> 2. 如果电感实际值为1.2mH和1.8mH，PI参数如何调整？
> 3. 电流环带宽能提高到多少？
>
> **参考答案：**
> 1. $K_{p\_d} = 1\text{mH} \times 1000 = 1, K_{p\_q} = 1.5\text{mH} \times 1000 = 1.5, K_i = 0.1 \times 1000 = 100$
> 2. 新$K_{p\_d} = 1.2$, 新$K_{p\_q} = 1.8$, Ki不变
> 3. 受采样频率限制，通常$\omega_c < f_s / 10$，$f_s = 10\text{kHz}$时$\omega_c < 1000 \text{ rad/s}$

### 练习2：计算题——转矩常数

> 已知PMSM参数：
> - 极对数 $P = 4$, 磁链 $\psi_f = 0.1\text{Wb}$, 额定电流 $I_q = 20\text{A}$
>
> **要求：**
> 1. 计算转矩常数$K_t$
> 2. 计算额定转矩
> 3. 如果是内置式PMSM($L_d=1\text{mH}, L_q=2\text{mH}$)，计算MTPA工作点
>
> **参考答案：**
> 1. $K_t = 1.5 \times 4 \times 0.1 = 0.6 \text{ Nm/A}$
> 2. $T_e = 0.6 \times 20 = 12 \text{ Nm}$
> 3. MTPA工作点需利用磁阻转矩，Id取负值

### 练习3：设计题——极对数测量

> 你拿到一个未知参数的PMSM，如何测量极对数？
>
> **要求：**
> 1. 设计测量方法
> 2. 列出所需工具
> 3. 给出计算公式
>
> **参考答案：**
>
> **方法1:手动旋转法**
> - 工具:示波器、编码器
> - 步骤:手动缓慢旋转电机一圈，观察电角度变化
> - 计算:$P = \Delta\theta_e / 360°$
>
> **方法2:反电动势法**
> - 工具:示波器、转速计
> - 步骤:电机匀速旋转，测量反电动势频率$f_e$和机械转速$n$
> - 计算:$P = 60 \times f_e / n$

### 练习4：诊断题——电流环震荡

> 场景:FOC控制电机，电流环震荡
>
> **现象：**
> - 电流波形震荡，频率约150Hz
> - 调小Kp，震荡减轻但仍存在
> - 调大Kp，震荡加剧
>
> **问题：**
> 1. 可能的原因有哪些？
> 2. 如何诊断？
> 3. 如何解决？
>
> **参考答案：**
> 1. 可能原因：电感参数不准、PI参数过大、采样延迟过大、滤波器截止频率过低、系统谐振
> 2. 诊断步骤：
>    - 实测电感值与配置值对比
>    - 计算理论PI值与实际值对比
>    - 测量PWM到ADC的延迟
>    - FFT分析电流频谱
> 3. 解决方案：
>    - 修正电感参数，重新计算PI
>    - 降低电流环带宽
>    - 增加相位补偿
>    - 陷波滤波器抑制谐振

### 练习5：选择题

**题目1**：表贴式PMSM的特点是？
- A. Ld > Lq  B. Ld < Lq  C. Ld ≈ Lq  D. Ld = 0
> 答案：C

**题目2**：FOC控制中，转矩公式是？
- A. Te = Kt × Ia  B. Te = 1.5 × P × ψf × Iq  C. Te = P × ψf × Id  D. Te = Kt × Iq²
> 答案：B

**题目3**：4极电机的极对数是？
- A. 1  B. 2  C. 4  D. 8
> 答案：B

**题目4**：弱磁控制时，Id电流应该？
- A. 为正值  B. 为负值  C. 为零  D. 与Iq相等
> 答案：B

**题目5**：MTPA控制适用于？
- A. 表贴式PMSM  B. 内置式PMSM  C. 感应电机  D. 直流电机
> 答案：B

---

> 深入理解 FOC 坐标变换推导，参见 [ALG-01-FOC-Theory](../algorithm/ALG-01-FOC-Theory.md)

## 附录：快速计算公式汇总

### A. 电角度与机械角度

$\theta_e = P \times \theta_m$
$\omega_e = P \times \omega_m$
$f_e = P \times n / 60$

### B. 反电动势

$E = K_e \times \omega_m = \psi_f \times P \times \omega_m$
$K_e = \psi_f \times P$

### C. 转矩

SPM: $T_e = 1.5 \times P \times \psi_f \times I_q$
IPM: $T_e = 1.5 \times P \times [\psi_f \times I_q + (L_d - L_q) \times I_d \times I_q]$
$K_t = 1.5 \times P \times \psi_f$

### D. 电流环带宽

$\omega_c = (R_s/L) \times (K_p/K_i)$（简化）
或 $\omega_c = 1 / \tau$（其中 $\tau = L/R$）

### E. 基速

$n_b = (60 / 2\pi) \cdot V_{dc} / (\sqrt{3} \cdot K_e)$  [rpm]

注：若$K_e$单位为V/(rad/s)，需乘$60/(2\pi)\approx 9.549$转换为rpm。若$K_e$单位为V/rpm，公式简化为$n_b=V_{dc}/(\sqrt{3}\cdot K_e)$

### F. 弱磁电流

$I_{d\_fw} = -\psi_f / L_d$（完全弱磁）

---

**文档信息**：
- 模块编号：HW-01
- 知识体系：电驱硬件原理
- 模块名称：电机本体基础
- 算法关联：电感→PI参数、磁链→转矩常数、极对数→Park变换

###  hpm_MC 代码关联

**电机参数映射** (`hpm_mcl_v2/hpm_mcl_physical.h`):
- `physical_motor_t`: res(相电阻), pole_num(极对数), vbus(母线电压), ld/lq/ls(电感), i_max(最大电流), flux(磁链), inertia(转动惯量), rpm_max(最高转速), hall(霍尔是否有Hall)
- 参数配置直接驱动 FOC 控制算法（Rs 用于电流估计，Ld/Lq 用于解耦，Flux 用于 SMO）
- hpm_mcl_v1 使用 `hpm_motor_para_t` (Rs/poles/Ls/额定参数)

参考: `SDK-03-HPM-MC-v2-Detect.md` — 离线参数检测可自动获取这些参数

>  检验你的理解：[HW-01 检验题目](./HW-01-assessment.md)

## 延伸实践
-  [路径11-2: 电机原理与硬件平台](../practice/PRACTICE-11-FOC-Engineering.md#站2) — PMSM数学模型+ODrive v3.6硬件对照

## 延伸学习
-  [HW-01B: 电机学物理本质深入](./HW-01B-Motor-Physics-Deep-Dive.md) — 磁路分析、绕组设计、转矩产生机制、dq轴物理本质、饱和与交叉耦合
